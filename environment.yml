name: cltr
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - numpy
  - pandas
  - scipy
  - matplotlib
  - plotly
  - pip
  - pip:
      - neurokit2
  - neurokit2
  - pip
  - pip:
      - -e .
